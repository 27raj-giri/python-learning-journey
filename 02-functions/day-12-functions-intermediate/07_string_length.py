# Write a function that returns the length of a string (without using len()).

def length(l):
    count = 0
    for char in text:
        count = count + 1

    return count

text = "Aayush"
print(length(len))

